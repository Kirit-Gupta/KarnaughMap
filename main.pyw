# input map size (2, 3, 4)
# input map location and values
# calc loop
# draw loop
# if expression is entered draw it on the map
# output simplified expression
from Code.GUI import GUI

start = GUI().run()